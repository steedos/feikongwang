name: buyer
label: 经办人
reference_to: space_users
reference_to_field: user
required: true
sort_no: 190
type: lookup
defaultValue: '{{global.userId}}'
amis:
  autoFill:
    "department": "${organization}"
